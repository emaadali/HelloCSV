import { ReactNode } from '../../shims/react-compat-shim.js';
interface Props {
    tooltipText: string;
    children: ReactNode;
    className?: string;
    hidden?: boolean;
}
export default function Tooltip({ tooltipText, children, className, hidden, }: Props): any;
export {};
//# sourceMappingURL=Tooltip.d.ts.map