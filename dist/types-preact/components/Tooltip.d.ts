import { ReactNode } from 'preact/compat';
interface Props {
    tooltipText: string;
    children: ReactNode;
    className?: string;
    hidden?: boolean;
}
export default function Tooltip({ tooltipText, children, className, hidden, }: Props): import("preact").JSX.Element;
export {};
//# sourceMappingURL=Tooltip.d.ts.map